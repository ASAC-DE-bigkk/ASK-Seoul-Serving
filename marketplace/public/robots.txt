# ASK: SEOUL — 서울 공공데이터 API 허브
#
# 문서·카탈로그는 열어 둔다. 색인되는 편이 이 서비스에 이롭고, 공개 데이터라 감출 것이 없다.
# API 경로는 막는다 — 크롤러가 부를 대상이 아니고(키가 필요하다), 미리보기는 인증이 없어
# 크롤러 트래픽이 그대로 분당 한도를 먹는다.

User-agent: *
Allow: /
Disallow: /api/
# 상태 페이지는 열 때마다 API 를 한 번 부른다 — 색인 대상이 아니다
Disallow: /status

# 상대 경로 유지 — 정적 파일은 dev·production 양쪽에 같은 내용으로 배포된다.
# (스펙은 절대 URL 을 권하지만, 환경별 절대 URL 을 박으면 다른 환경 배포가 오염된다.
#  정식 공개 시 호스트별 생성이 필요해지면 워커 런타임 생성으로 전환 — 런북 §6)
Sitemap: /sitemap.xml

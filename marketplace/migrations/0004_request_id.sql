-- 요청 ID 를 로그에 남긴다 — 응답 헤더 X-Request-Id 로 받은 값을 지원 문의에서
-- 그대로 조회할 수 있어야 "그 요청"을 특정할 수 있다. 없으면 시각·경로로 추정해야 하고,
-- 같은 초에 같은 경로를 여러 번 부른 경우는 끝내 못 가린다.
--
-- 기존 행은 NULL 로 남는다 — 0 이나 빈 문자열로 채우지 않는다. NULL 은 "이 컬럼이
-- 생기기 전 요청"이라는 사실이고, 빈 문자열은 "ID 가 없는 요청"이라는 거짓말이 된다.
ALTER TABLE _request_log ADD COLUMN request_id TEXT;
